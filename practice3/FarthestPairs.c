#include <stdio.h>

int main()
{
    int n, nextline = 0;
    while(1)
    {
        scanf("%d", &n);
        if(n == 0)
            break;
        if(nextline++ != 0)
            printf("\n");

        int
        int array[n];
        for(i = 0; i < n; i++)

    }
}
